Eduardo Úcar Hernández ||1ºDAM|| EJERCICIOS BÁSICOS GIT

Crea una carpeta en tu equipo.
mkdir ejerciciogit

Inicia en la carpeta anterior un repositorio de git.
cd ejerciciogit
git init

Añade 5 ficheros de texto: ciudades europeas, ciudades americanas, provincias españolas,
paises de la UE, paises fuera de la UE.
Creamos los ficheros por medio de la interfaz grafica de Windows (archivos de texto .txt).

Añade contenido en todos los ficheros anteriores y haz commit.
He escrito con Notepad++ dentro de los ficheros de texto generados en el paso anterior.
add .
git commit -m "Ejercicios básicos GIT

Crea otra rama (llámala develop por ejemplo) y cámbiate a ella.
git branch develop
git checkout develop ( o también podríamos hacerlo en un paso con git checkout -b develop)

Añade contenido en los ficheros del repositorio y haz commit.
He escrito con Notepad++ dentro de los ficheros de texto generados en el paso anterior.
add .
git commit -m "Ejercicios básicos GIT"

Vuelve a la rama anterior (master) y añade más contenido a los ficheros.
git checkout master
Vuelvo a añadir contenido con Notepad++.

Fusiona (merge) las ramas que tienes. Si aparecen conflictos, resuélvelos. ¿Qué has visto en los
ficheros del repositorio?
git merge develop (situados en master)

Crea un tag o etiqueta de la versión fusionada.
git tag 'tagGIT'

Sube a tu cuenta de github el repositorio. Deberás primero crear un repositorio vacío (sin
ficheros) en tu cuenta de github y seguir las instrucciones de la página.
Añade un fichero .gitignore a tu repositorio que tenga en cuenta que tu repositorio se va a
manejar desde windows y en él se deberían ignorar los ficheros propios de windows, así
como .pdf y .doc
gitignore.io(accedo desde el navegador)
Escribo Windows y MicrosoftOffice y le doy a Create. Después creo un txt, y pego el texto
generado en la web
Aquí añado #pdf y justo debajo *.pdf.

Copia en la carpeta de tu repositorio un documento de word y un pdf.
Creo los dos archivos con el formato correspondiente.
Control+C
Control+V en la carpeta del repositorio.

Haz commit y push del contenido del repositorio. ¿Qué ha ocurrido?
git add .
git commit -m "Ejercicios básicos GIT"

Investiga los comandos de git: log, diff y status. ¿Qué hacen?
Pon en un fichero de texto los comandos de git que has introducido en cada ejercicio junto con
las respuestas a las cuestiones que se plantean y añádelo a tu repositorio. Haz commit, push y
envíame el enlace por email.